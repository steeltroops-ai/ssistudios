# Redirect npm commands to use Bun
# This ensures all npm commands use Bun instead
auto-install-peers=true
prefer-offline=true

# Bun compatibility settings
registry=https://registry.npmjs.org/
save-exact=false
package-lock=false
